# Copy this file to .env.local and fill in your own ntfy.sh topic.
# .env.local is gitignored (matched by *.local). The build inlines this value,
# so it is NOT a secret — it just keeps the topic out of the repo source.
VITE_NTFY_TOPIC=adv-your-topic-here
